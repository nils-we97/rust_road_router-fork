use std::error::Error;

/// Executes a given set of pre-generated Dijkstra-Rank Queries with different potential approaches
/// The query results will be stored as a CSV file (<output_name>.csv) for further visualization (e.g. with R)
///
/// Additional parameters: <path_to_graph> <path_to_queries> <output_name>
fn main() -> Result<(), Box<dyn Error>> {
    Ok(())
}
