use std::error::Error;
use std::path::Path;

/// preprocesses a given graph directory and removed invalid nodes and edges
pub fn preprocess_graph(_graph_directory: &Path) -> Result<(), Box<dyn Error>> {
    //TODO

    Ok(())
}
